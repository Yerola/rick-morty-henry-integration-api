const app = require("./src/App")

const PORT = 3001;

app.listen(PORT, () => {
  // console.log(`http://localhost:${PORT}`);
  console.log("Back http://localhost:3001 levantado con éxito!");
});
